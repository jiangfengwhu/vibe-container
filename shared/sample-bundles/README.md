# Sample Bundles

The runnable Flutter assets live under:

```text
assets/sample_bundles/
```

They are packaged by Flutter and imported into the app documents directory at runtime. This shared folder is reserved for backend fixtures once generated bundles are exported as zip archives.
